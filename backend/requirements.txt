# ── FastAPI core ──────────────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.27.0
python-multipart
starlette

# ── Database ──────────────────────────────────────────────────────────────────
mysql-connector-python
sqlalchemy

# ── Auth ──────────────────────────────────────────────────────────────────────
python-jose[cryptography]
passlib[bcrypt]
python-dotenv==1.0.0
email-validator

# ── LangChain + LangGraph + Gemini ────────────────────────────────────────────
langchain>=0.1.0
langchain-google-genai
langchain-community
langchain-text-splitters
google-generativeai
langgraph>=0.2.0

# ── Embeddings (CPU ONLY — évite PyTorch GPU = 1.3 GB inutile) ───────────────
# Torch CPU uniquement
--extra-index-url https://download.pytorch.org/whl/cpu
torch
sentence-transformers
langchain-huggingface

# ── Vector Store ──────────────────────────────────────────────────────────────
chromadb

# ── Notifications & PDF ───────────────────────────────────────────────────────
sib-api-v3-sdk
reportlab
arabic-reshaper
python-bidi
apscheduler

# ── Maps ──────────────────────────────────────────────────────────────────────
requests

# ── Utilitaires ───────────────────────────────────────────────────────────────
pydantic>=2.8.0
pydantic-settings>=2.3.0